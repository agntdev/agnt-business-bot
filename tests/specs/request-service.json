[
  {
    "name": "Request service shows service options",
    "steps": [
      {
        "send": { "callback": "request:service" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "What do you need help with?" } }
        ]
      }
    ]
  }
]
