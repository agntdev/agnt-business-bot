[
  {
    "name": "Book time shows date options",
    "steps": [
      {
        "send": { "callback": "booking:start" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Pick a date for your booking:" } }
        ]
      }
    ]
  }
]
