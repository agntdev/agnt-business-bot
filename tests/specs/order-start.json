[
  {
    "name": "Place order shows plan options",
    "steps": [
      {
        "send": { "callback": "order:start" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Choose a plan:" } }
        ]
      }
    ]
  }
]
